W02 — RAILWAY INITIAL DEPLOYMENT

Platform: Railway
Service name: signup-readiness-pakistan
Expected final Railway domain:
https://signup-readiness-pakistan.up.railway.app/

THIS IS THE INITIAL APPROVAL BUILD.
It is intentionally NOINDEX in both:
- HTML robots meta
- HTTP X-Robots-Tag generated by server.js

Recommended deployment route:
A) GitHub + Railway dashboard
1. Create a new GitHub repository.
2. Upload the complete contents of this package to the repository root.
3. Railway → New Project → Deploy from GitHub repo.
4. Choose the repository.
5. Railway/Railpack will detect package.json and run `npm start`.
6. Wait for the healthcheck to pass.
7. Service → Settings → Networking → Public Networking → Generate Domain.
8. Use service name `signup-readiness-pakistan`. Expected domain is `https://signup-readiness-pakistan.up.railway.app/`.
9. If Railway generates a different `*.up.railway.app` host, DO NOT approve/index yet. Send the exact live URL so the canonical/schema/sitemap can be aligned in the final package.

Alternative CLI route:
1. Extract this ZIP.
2. `railway login`
3. `railway init`
4. Name the project/service: signup-readiness-pakistan
5. `railway up`
6. `railway domain`
7. If necessary, Railway supports renaming a Railway-provided domain with:
   `railway domain update OLD-DOMAIN.up.railway.app --domain signup-readiness-pakistan`

LIVE APPROVAL CHECK:
[ ] Homepage opens
[ ] Home appears in menu
[ ] Three article images fit correctly
[ ] Floating Get New ID button stays bottom-right
[ ] About / Editorial Policy / Privacy / Responsible Use / Contact work
[ ] /sitemap.xml opens
[ ] /robots.txt opens
[ ] Canonical matches the live Railway domain
[ ] HTML source contains noindex,follow
[ ] HTTP response has X-Robots-Tag: noindex, follow
[ ] No horizontal overflow / giant SVG

After approval:
Send the exact GSC verification meta tag.
Then one final W02 package will:
- insert GSC verification
- switch HTML robots to index,follow
- switch server.js REVIEW_MODE to false
- retain sitemap/robots/schema/canonical
